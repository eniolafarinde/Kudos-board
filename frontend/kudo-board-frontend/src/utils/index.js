export function getBaseUrl() {
    return "https://kudos-board-fpm6.onrender.com"
    
    return (

        import.meta.env.VITE_DEV ? "http://localhost:3000" : "https://kudos-board-fpm6.onrender.com"

    )
}